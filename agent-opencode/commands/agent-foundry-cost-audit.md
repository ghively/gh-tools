---
description: Audit agent token spend and propose routing, caching, context, retry, and model changes with estimated savings.
agent: plan
---

Cost-audit `$ARGUMENTS`. Load `model-selection`, especially cost-tracking and
routing-architecture. Locate every model call, retry, fan-out, context assembly
and cache setting with file:line evidence. Build a per-run and monthly cost
model, rank leaks, and propose fixes with arithmetic, quality risk, and eval
verification. Re-check current pricing before quoting high-stakes figures.
